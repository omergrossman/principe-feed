macOS Vulnerability Allows Non-Privileged Security Tool Disablement

A macOS flaw enables attackers to disable security and browser protection features without requiring administrator credentials or kernel-level exploits. The vulnerability reduces the attack surface needed for compromise, as adversaries can leverage standard user access to circumvent built-in protections.

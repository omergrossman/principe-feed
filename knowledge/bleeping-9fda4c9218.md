Malicious Edge Extension Exploits Native Messaging to Deploy Backdoor

A malicious Microsoft Edge extension named 'Edgecution' leverages the browser's native messaging feature to circumvent sandbox restrictions and install a Python-based backdoor. The technique demonstrates how browser extensions can serve as infection vectors by establishing communication channels between the browser and system processes.

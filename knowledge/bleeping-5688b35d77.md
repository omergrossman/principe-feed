Over 8,300 Gitea servers remain vulnerable to active code execution exploitation

Shadowserver identified more than 8,300 publicly accessible Gitea instances that have not applied patches for a critical remote code execution vulnerability currently being exploited in the wild. The unpatched deployment rate suggests delayed patch adoption across the exposed infrastructure.

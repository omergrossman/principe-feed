Cloudflare Addresses Container Isolation Vulnerability

Cloudflare patched a flaw in its Containers service that allowed customers to access residual disk data from previously-terminated containers on shared infrastructure. The exposed data was from deallocated storage rather than active workloads, and access was non-deterministic. The vulnerability has been remediated.

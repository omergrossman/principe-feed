NVIDIA OpenClaw Networking Vulnerability Enables LLM Model Poisoning

A networking flaw in NVIDIA's OpenClaw tool permits unauthenticated access to local model servers via the Ollama API, potentially allowing attackers to corrupt AI agents persistently. The vulnerability could be exploited to modify model behavior or inject malicious logic into language model deployments.

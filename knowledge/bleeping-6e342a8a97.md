ShinyHunters compromised Clop ransomware leak site via Grav CMS vulnerability

The Clop ransomware operation's data leak site was breached and defaced by ShinyHunters through an unpatched path traversal vulnerability in Grav CMS that requires no authentication. The gang has since migrated the site to a new Tor address following the incident.
